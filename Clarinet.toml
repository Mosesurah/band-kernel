[project]
name = "band-kernel"
authors = ["Band Kernel Team"]
description = "A modular DAO governance framework for decentralized organizational management"
telemetry = false
requirements = []
[contracts.kernel-registry]
path = "contracts/kernel-registry.clar"
depends_on = []

[contracts.kernel-proposals]
path = "contracts/kernel-proposals.clar"
depends_on = []

[contracts.kernel-treasury]
path = "contracts/kernel-treasury.clar"
depends_on = []

[contracts.kernel-voting]
path = "contracts/kernel-voting.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
